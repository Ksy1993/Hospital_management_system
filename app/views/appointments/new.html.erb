
  <%= form_for @appointment do |appointment|%>
 <%= appointment.collection_select(:doctor_id, Doctor.all, :id, :name,prompt: "Please select doctor") %><br><br>

 <%= appointment.collection_select(:patient_id, Patient.all, :id, :name,prompt: "Please select patient") %><br><br>
 Date and Time of Appointment: <%= appointment.text_field :time%><br><br>
   <%= appointment.submit %>
   <%end%>